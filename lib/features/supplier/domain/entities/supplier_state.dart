/// Enum representing the operational state of a supplier.
enum SupplierState {
  /// Supplier is active and accepting orders
  active,

  /// Supplier is inactive and not accepting orders
  inactive,
}
